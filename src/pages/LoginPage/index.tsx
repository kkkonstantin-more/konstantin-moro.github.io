import React from 'react';

import useStyles from './style';

const LoginPage = () => {
  const classes = useStyles();

  return <div className={classes.root}>Initial setup</div>;
};

export default LoginPage;
