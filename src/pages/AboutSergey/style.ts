import { makeStyles } from '@material-ui/core';

export default makeStyles({
  root: {
    whiteSpace: 'pre-line',
    fontSize: '3.5rem',
    color: 'white',
    fontFamily: "'Marck Script', cursive",
  },
});
